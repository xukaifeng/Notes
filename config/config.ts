import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Notes',
  mode: 'site',
  hash: true,
  description: 'test',
  // more config: https://d.umijs.org/config
  menus: {},
});
